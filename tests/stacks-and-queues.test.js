'use strict';

const classes = require('../libs/stacksAndQueues')
const Stack = classes.Stack;
const Node = classes.Node;

describe(`Test to verify the proper creation of the Stack class and it's instances`, () => {

  it(`should create an empty instance of a stack`, () => { });

  it(`should assign the 'top' to be null when first creted`)

});

